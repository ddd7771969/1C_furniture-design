&НаКлиенте
Процедура ЗаголовокЗакладки(Элементы, ИмяЗакладки, Заголовок, ТабличнаяЧасть, ТабличнаяЧасть1 = Неопределено) Экспорт

	КоличествоСтрок = ТабличнаяЧасть.Количество() + ?(ТабличнаяЧасть1 = Неопределено, 0, ТабличнаяЧасть1.Количество());
	
	Если КоличествоСтрок = 0 Тогда
	
		стрКоличество = "";	
	
	Иначе
	
		стрКоличество = " (" + КоличествоСтрок + ")"; 
	
	КонецЕсли;
	
	Элементы[ИмяЗакладки].Заголовок = Заголовок + стрКоличество; 

КонецПроцедуры // ЗаголовокЗакладки()

