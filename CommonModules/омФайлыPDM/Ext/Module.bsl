Функция ПолучитьТабличныйДокументСтруктураФайлов(КореньЗапроса = "") Экспорт
	 
	 COMОбъект = омPDMНаСервереПовтор.ПодключитьБиблиотекуPDM();
	 
	 Если COMОбъект = Неопределено Тогда
	 
	 	Возврат Неопределено
	 
	 КонецЕсли;
	 
	 Если ПустаяСтрока(КореньЗапроса) Тогда
		 КореньЗапроса = COMОбъект.Root_folder();
	 КонецЕсли;
	 
	 ХМЛФайлыПапки = COMОбъект.Files_folder(КореньЗапроса); 
	 
	 Если НЕ ЗначениеЗаполнено(ХМЛФайлыПапки) Тогда
	 
	 	Возврат Неопределено;	
	 
	КонецЕсли;
	

//ХМЛФайлыПапки ="<files_folders>
//			|  <file_folders name=""EFICAD"">
//			|    <local_path>C:\PDM\EFICAD</local_path>
//			|    <ID>306</ID>
//			|    <this_folder>True</this_folder>
//			|  </file_folders>
//			|  <file_folders name=""Проекты"">
//			|    <local_path>C:\PDM\Проекты</local_path>
//			|    <ID>793</ID>
//			|    <this_folder>True</this_folder>
//			|  </file_folders>
//			|  <file_folders name=""&lt;items&gt;"">
//			|    <local_path>C:\PDM\&lt;items&gt;</local_path>
//			|    <ID>822</ID>
//			|    <this_folder>True</this_folder>
//			|  </file_folders>
//			|  <file_folders name=""БМ"">
//			|    <local_path>C:\PDM\БМ</local_path>
//			|    <ID>8832</ID>
//			|    <this_folder>True</this_folder>
//			|  </file_folders>
//			|  <file_folders name=""1C"">
//			|    <local_path>C:\PDM\1C</local_path>
//			|    <ID>13720</ID>
//			|    <this_folder>True</this_folder>
//			|  </file_folders>
//			|</files_folders> ";	
	
	ЧтениеXML = Новый ЧтениеXML;
	ЧтениеXML.УстановитьСтроку(ХМЛФайлыПапки);
	
    Пока ЧтениеXML.Прочитать() Цикл
      Если ЧтениеXML.ИмяЭлемента = "Контрагент" И ЧтениеXML.ТипУзла = ТипУзлаXML.НачалоЭлемента Тогда
            
            // Сбрасываем значения для нового контрагента
            ТекНаименование = "";
            ТекИНН = "";
            ТекКПП = "";
            
        ИначеЕсли ЧтениеXML.ИмяЭлемента = "Наименование" И ЧтениеXML.ТипУзла = ТипУзлаXML.НачалоЭлемента Тогда
            ЧтениеXML.Прочитать();
            ТекНаименование = ЧтениеXML.Значение;
            
        ИначеЕсли ЧтениеXML.ИмяЭлемента = "ИНН" И ЧтениеXML.ТипУзла = ТипУзлаXML.НачалоЭлемента Тогда
            ЧтениеXML.Прочитать();
            ТекИНН = ЧтениеXML.Значение;
            
        ИначеЕсли ЧтениеXML.ИмяЭлемента = "КПП" И ЧтениеXML.ТипУзла = ТипУзлаXML.НачалоЭлемента Тогда
            ЧтениеXML.Прочитать();
            ТекКПП = ЧтениеXML.Значение;
            
        // Обрабатываем конец элемента "Контрагент" - создаем элемент
        ИначеЕсли ЧтениеXML.ИмяЭлемента = "Контрагент" И ЧтениеXML.ТипУзла = ТипУзлаXML.КонецЭлемента Тогда
            
            // Создаем нового контрагента
            Попытка
                Если Не ПустаяСтрока(ТекНаименование) Тогда
                    НовыйКонтрагент = Справочники.Контрагенты.СоздатьЭлемент();
                    НовыйКонтрагент.Наименование = ТекНаименование;
                    НовыйКонтрагент.ИНН = ТекИНН;
                    НовыйКонтрагент.КПП = ТекКПП;
                    НовыйКонтрагент.Записать();
                    Сообщить("Добавлен: " + ТекНаименование);
                КонецЕсли;
            Исключение
                Сообщить("Ошибка при создании контрагента: " + ТекНаименование);
            КонецПопытки;
            
        КонецЕсли;
    КонецЦикла;
 
КонецФункции // ПолучитьТабличныйДокументСтруктураФайлов()

Функция ИнформацияОФайлеPDM(ИД, АдресРезультата = Неопределено) Экспорт

	 COMОбъект = омPDMНаСервереПовтор.ПодключитьБиблиотекуPDM();
	 
	 Если COMОбъект = Неопределено Тогда
	 
	 	Возврат Неопределено
	 
	КонецЕсли;
	
	СтрокаXML = COMОбъект.Get_file_information(ИД);
	
	Если АдресРезультата = Неопределено Тогда
		
		Возврат СтрокаXML;
		
	КонецЕсли;
	
	Возврат ПоместитьВоВременноеХранилище(СтрокаXML, АдресРезультата);	
	
КонецФункции // ИнформацияОФайлеPDM()

Функция ПолучитьПодПапкиИФайлыИзПапки(ПутьКФалу, АдресРезультата = Неопределено, ПолныйПуть = Истина) Экспорт

	 COMОбъект = омPDMНаСервереПовтор.ПодключитьБиблиотекуPDM();
	 
	 Если COMОбъект = Неопределено Тогда
	 
	 	Возврат Неопределено
	 
	КонецЕсли;
	
	СтрокаXML = COMОбъект.Files_folder(ПутьКФалу, ПолныйПуть);
	
	Если АдресРезультата = Неопределено Тогда
		
		Возврат СтрокаXML;
		
	КонецЕсли;
	
	Возврат ПоместитьВоВременноеХранилище(СтрокаXML, АдресРезультата);	

КонецФункции // ПолучитьПодПапкиИФайлыИзПапки(ПутьКФалу)
