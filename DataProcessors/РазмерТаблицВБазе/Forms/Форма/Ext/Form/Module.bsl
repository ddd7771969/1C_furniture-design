
&НаСервереБезКонтекста
Функция РазмерТаблицНаСервере()

	МассивОбъектов = Новый Массив();
	МассивОбъектов.Добавить("Справочник.ИзделияКомплект");
	
	Размер = ПолучитьРазмерДанныхБазыДанных(Новый Массив(), МассивОбъектов);

КонецФункции

&НаКлиенте
Процедура РазмерТаблиц(Команда)
	
	РазмерТаблицНаСервере();
	
КонецПроцедуры
