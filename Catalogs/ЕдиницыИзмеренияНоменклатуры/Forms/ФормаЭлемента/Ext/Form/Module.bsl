
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Если НЕ ПолучитьФункциональнуюОпцию("ИспользоватьМножитель") Тогда
		ЭтаФорма.Элементы.Множитель.Видимость = Ложь;
		Объект.Множитель = 1;
	КонецЕсли;
КонецПроцедуры
