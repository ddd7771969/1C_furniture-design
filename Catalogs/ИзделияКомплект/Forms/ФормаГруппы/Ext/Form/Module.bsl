
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Элементы.Проект.ТолькоПросмотр = НЕ РольДоступна("АдминистраторСистемы");	
	
	Если омИзделиеКомплект.ПервыйУровень(Объект.Ссылка) Тогда
		Элементы.Композиция.Видимость = Истина;
	Иначе
		Элементы.Композиция.Видимость = Ложь;
	КонецЕсли;
	
КонецПроцедуры
